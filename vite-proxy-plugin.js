/**
 * This plugin is no longer needed - using Vite's built-in proxy instead.
 * See vite.config.js for the proxy configuration.
 */
export default function apiProxyPlugin() {
  return {
    name: 'api-proxy-placeholder',
  };
}
